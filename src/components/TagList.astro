---
/**
 * 标签列表组件
 * 渲染一组文章标签
 *
 * 使用方式：
 *   <TagList tags={["HTML", "CSS", "新手教程"]} />
 */
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
// base 路径：开发时为 /，Gitee Pages 部署时为 /QQ_Web_Blog/
const base = import.meta.env.BASE_URL === '/' ? '/' : import.meta.env.BASE_URL + '/';
---

<div class="tag-list">
  {
    tags.map((tag) => (
      <a href={`${base}search/?q=${encodeURIComponent(tag)}`} class="tag">
        {tag}
      </a>
    ))
  }
</div>

<style>
  .tag-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }
</style>
